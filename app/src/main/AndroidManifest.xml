<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student_digiassist">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Attendance.AddAttendance"></activity>
        <activity android:name=".Attendance.Attendances" />
        <activity android:name=".AddProject" />
        <activity android:name=".AddTask" />
        <activity android:name=".AddExpense" />
        <activity android:name=".MainPage" />
        <activity android:name=".competition.Alerts" />
        <activity android:name=".notes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotifierAlarm" />
    </application>

</manifest>